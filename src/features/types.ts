export type MyFormData = {
  primitive: string;
  quantity: number;
  required1: string;
  required2: string;
  required3: string;
  required4: string;
  nested: {
    value: string;
  };
  object: {
    key: string;
  };
};
